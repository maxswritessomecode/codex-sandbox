# Rename Notes

The project was renamed from `cc-sandboxes` / `mcp-lab` to **Codex Sandbox**.

Recommended naming:

- Repository: `codex-sandbox`
- Display name: `Codex Sandbox`
- Command: `codex-sandbox`
- Python entry file: `codex_sandbox.py`
- Tagline: `Disposable workspaces for Codex agents, MCP tools, and plugin testing.`

The original local sandbox state contains private runtime files and should not be committed directly.
